import setuptools

setuptools.setup(
    name='pipeline-trial',
    version='1.0',
    install_requires=[],
    packages=setuptools.find_packages(),
    include_package_data=True,

    # warnings suppressors
    description='Pipeline on Dataflow Demo',
    author='MB',
    author_email='MB@apache.beam',
    url='https://foo.bar/',)